# Update Log

## 2026-07-07
* **MFDB — flagged the two `bootstrap_*` seeders as intentional-raw (PRD-26 audit).** `provenance/operation_parameters.py` and `lifecycle/lifecycle.py` seed their def/rule/vocabulary tables from authored JSON on a **bare `sqlite3.Connection`** (no `MFDatabase`/`dao` in scope) via a bespoke bulk-reseed: `INSERT OR IGNORE` vocabulary, delete-by-non-PK per `operation_type`/`entity_type`, `MAX(def_id|rule_id)` reindex, then bulk insert with explicit keys. These are aggregate/bulk-delete shapes the DAO cannot express and are not scattered CRUD, so they stay raw with an explicit `# raw:` marker rather than churning them onto a per-call-introspected DAO. Reclassified in the audit as bespoke (insert 101 → 98, delete 17 → 15, bespoke 45 → 50). Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MFDB — converted the two convertible writes in `samples/seed_data.py` (PRD-26 burn-down).** The seed builder already used repository methods for most writes; rerouted the last two hand-SQL writes onto the DAO: the post-`add_probe` `UPDATE probes SET verification_status/quality` → `db.dao.update("probes", probe_id, …)`, and the precomputed-Förster-radius `INSERT OR REPLACE INTO flr_fret_forster_radius` → `db.dao.upsert(..., conflict=["forster_radius_id"])`. The table carries two UNIQUE constraints (`forster_radius_id` and the `(sample_id, donor_probe_id, acceptor_probe_id)` triple); the seed IDs are deterministic so both point at the same row on re-seed, making the single-target upsert faithful — and identity-preserving where `INSERT OR REPLACE` would delete+reinsert (rotating the autoincrement `id`). Dropped the now-unused `_utc_now` import. Standalone suite **419 passed, 1 skipped**. Audit: insert 102 → 101, update 76 → 75. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MFDB — purged scattered CRUD SQL from `samples/sample_manager.py` (PRD-26 burn-down).** Continued the "no scattered raw SQL" migration ([PRD-26](/prds/prd-26.md); tracker [specs/mfdb-sql-audit.md](/specs/mfdb-sql-audit.md)) into the non-query modules, where hand-written SQL is the real antipattern. Rerouted every **write** in `sample_manager.py` — the sample-graph builder — off `db.conn.execute` and onto existing repository methods / the dictionary DAO: probe positions → `db.add_poly_probe_position`, sample conditions → `db.add_sample_condition`, PDBx/flrCIF key-values (the keyless `flr_sample_key_value` table) → `db.set_sample_key_value` (whose raw upsert stays centralized in `SampleMixin`), and optical-properties / spectra inserts → `db.dao.insert` with skip-if-present guards via `db.dao.list` (preserving the "don't clobber a reused probe's first values" semantics and the JSON-text spectra serialization that this sample path reads back with `json.loads`, distinct from the numpy-blob `add_spectrum` path). Dropped the now-dead module-local `_utc_now` helper (the DAO/repository methods stamp `created_at`/`updated_at`). The only raw SQL left is reads plus one bespoke 4-column position-existence probe, flagged `# raw`. Standalone suite **419 passed, 1 skipped**. Audit totals: insert 109 → 102. Advances [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).

## 2026-07-06
* **MFDB — god-class breakup: `repository.py` 6,397 → 1,848 lines (−71%).** Resumed and largely completed the Phase-4 [PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05) decomposition of the `MFDatabase` god-class into per-concern query mixins under `queries/`. Extracted six new mixins this pass — **ProbeMixin** (probe/spectra/optical/Förster), **SampleMixin** (samples/entities/positions/linkage), **SetupCalibMixin** (setups/calibrations), **ObjectStoreMixin** (object store + materialization), **ArtifactOpsMixin** (the artifact/operation/edge/provenance-graph core, ~46 methods), and **AnalysisMixin** (analysis runs/products/metadata) — for **13 mixins** total. Built a reusable AST-boundary method extractor (handles multi-line signatures, decorators, and leaves class attributes in place); relocated module-level helpers (`_default_reference_spectra_path`→`store/database_resolver`, `_validate_checksum`→`schema/_sqlutil`) to avoid repository↔mixin import cycles, and carried class-level method aliases (`save_setup_snapshot`, `link_operation_artifact`) into their mixins. Public `MFDatabase` surface unchanged (methods resolve via the MRO); standalone suite **419 passed** after every extraction. `repository.py` is now a thin composition over `self.conn`/`self.dao`/`self.lineage` — what remains is the legitimate core (init/connection/properties, migration, audit, vocabulary, experiment key-values, pdbx metadata).
* **MFDB — admin GUI moved out; `adapters/` subpackage added.** Continued the clean-bounds migration: relocated the chisurf-coupled admin **GUI** (24 modules + 7 view-spec JSONs) out of the package into the ChiSurf plugin (`chisurf/plugins/core/mfdb_admin/gui/`), replacing the compat wrappers and retargeting 38 importers + the manifest entrypoint; the admin **backend** (RPC handlers) stays in `mfdb.admin` and is chisurf-free. Added an **`adapters/`** subpackage as the home for external-system bridges — moved `chinet_adapter` → `mfdb.adapters.chinet` (ChiSurf fit sessions → MFDB), with eLabFTW / electronic-lab-notebook adapters to follow; adapters keep target-system imports lazy so the core still imports standalone. Top-level `mfdb` is now 5 files. Standalone: 419 passed. Advances [PRD-24](/prds/prd-24.md)/[PRD-48](/prds/prd-48.md).
* **MFDB — own hermetic test suite + concern-subpackage reorg (PRD-24).** Gave the package clear bounds and a maintainable file layout. (1) **Relocated ~35 pure-MFDB test modules** from ChiSurf `test/fio`+`test/plugins` into `modules/mfdb/tests/` with a hermetic conftest (isolation via MFDB's own `MFDB_SETTINGS_DIR`, no chisurf import); the suite runs standalone (`cd modules/mfdb && PYTHONPATH=src pytest`). This immediately exposed **real boundary violations** in the package — fixed the module-level ones (`admin/backend/services` used `from chisurf import logging` → stdlib; `{measurement,ndxplorer}_services` imported `chisurf.server.services` error helpers → new `mfdb.admin.backend._service_errors`). ChiSurf↔MFDB integration tests (admin GUI `MFDBClient`, chinet/`parameter_registry` alignment, ndxplorer submodule, chisurf test-data) stay in the ChiSurf tree. (2) **Reorganized the 38 flat top-level modules into concern subpackages** — `schema/ store/ provenance/ samples/ lifecycle/ security/ project/` — leaving 6 core files at top (`repository`, `api`, `models`, `config`, `chinet_adapter`, `__init__`); collapsed the `auth/`-only-`__init__` "package" to `auth.py`. Hard cutover (no compat shims): every internal, test, and ChiSurf import retargeted to grouped paths (`mfdb.schema.dao`, `mfdb.provenance.result_registry`, …); fixed `Path(__file__).parent/"data"` resolution in the seven moved modules that read bundled data. **Proof of clean bounds: 419 passed with only `src` on the path** (no chisurf, no chinet). ChiSurf `test/fio`: 236 passed, 1 pre-existing failure (`test_ndxplorer_cli`, needs the ndxplorer submodule). Advances [PRD-24](/prds/prd-24.md); layout documented in [architecture/mfdb.md](/architecture/mfdb.md).
* **MFDB — reimplemented external-reference persistence (PRD-39); test/fio 7 → 1.** The struct_ref/external-reference write path was lost when the SQLAlchemy `orm/` stack was deleted (Merge 1) and never reproduced on the raw-SQL path — `create_sample` recorded entities but silently dropped their UniProt/PDB cross-references and mutations. Reimplemented minimally: `sample_manager._persist_entity_external_refs` records `uniprot_accession`→`struct_ref(db_name=UNP)`, `pdb_id`→`struct_ref(db_name=PDB)`, the primary reference's sequence alignment as `struct_ref_seq`, and engineered mutations (given, or auto-diffed from `reference_sequence` via `external_refs.diff_sequences`) as `struct_ref_seq_dif`; `get_sample_full_description` now surfaces per-entity `external_refs`/`mutations`, and the probe↔mutation export validator resolves the entity via the position's `entity_id` (added to the read) with a `residue_number`/`residue_name` fallback for `seq_id`/`comp_id`. **Writes go through the dictionary-driven `DictionaryDao` (`db.dao.insert`/`list`), not hand SQL** — and codified that: raw/f-string SQL for CRUD is now documented as an **antipattern** in [architecture/mfdb.md](/architecture/mfdb.md) (one engine = `db.dao`; raw SQL only for bespoke joins/graph/aggregate reads). Also fixed a latent `_array_to_text` `TypeError` (np.frombuffer on a text blob) in the flrCIF export path. `test/fio` now **656 passed / 1 failed**; the lone remaining failure (`test_ndxplorer_cli`) is an out-of-scope environment issue (needs the `ndxplorer` submodule built). Advances [PRD-19](/prds/prd-19.md)/[PRD-26](/prds/prd-26.md)/[INC-05](/specs/assessment.md#inc-05).
* **MFDB simplification — post-cutover stabilization: test/fio 179 → 7 failures.** The Merge-3 cutover exposed a broken full suite; root-caused and fixed it in committed batches. **The dominant cause (~150 cascading failures) was a single global-state leak:** `test_fcs_setup.py`'s v36-migration test set the module global `schema.SCHEMA_VERSION = 35` and never restored it, so every later test's fresh `MFDatabase` skipped the version-40 reconcile-to-`.dic` step and came up with a partial schema (missing `mfdb_state_transition`, `mfdb_operation.protocol_id`). Removed that obsolete test (no v36 migration exists after the PRD-19 collapse to `MIGRATIONS={1,40}`). Then fixed the independent failures: a **live default-user-id bridge** ([PRD-24](/prds/prd-24.md)) — `mfdb.config.set_default_user_id_resolver` consulted by `configured_default_user_id`, registered from `chisurf.core.settings` to read `cs_settings` on each call (MFDB stays standalone); a real **`get_parameter` bug** (delegated to `dao.get` on the table PK `parameter_id` instead of the `parameter_uuid` column → always None); `importer.py` `get_probe_by_id`→`get_probe`; `migrate_schema` returns no report for a fresh (from-scratch) database; and a batch of stale-reference test fixes after the cutover (flr_ext `.dic` via `MmcifDictionary.DATA_DIR`, `python -m mfdb.pdbx_metadata`, `mfdb.admin.backend.services`, admin session-token minting now that a fresh DB seeds a `user_default` admin, stale `get_optical_properties`/`get_spectrum` shapes, explicit plugin-manifest path). **Remaining 7:** `test_external_refs` (6) is a real **PRD-39 feature reimplementation** — struct_ref/external-reference persistence (uniprot/pdb→`struct_ref`, mutations→`struct_ref_seq_dif`, sequence auto-diff, export validation, and surfacing `external_refs`/`mutations` in `get_sample_full_description`) was lost when the SQLAlchemy `orm/` stack was deleted in Merge 1 and never reimplemented on the raw-SQL path; `test_ndxplorer_cli` (1) is an out-of-scope environment issue (needs the `ndxplorer` submodule built). Advances [INC-05](/specs/assessment.md#inc-05)/[PRD-24](/prds/prd-24.md).
* **MFDB simplification — Merge 3 cutover landed: one import namespace, dead shim gone.** Committed the previously-uncommitted cutover in three coherent local commits. (1) Deleted the shadowed `chisurf/core/mfdb/*.py` shim modules — the facade `__init__.py` already routes `chisurf.core.mfdb.X → mfdb.X` via its meta-path `_AliasFinder`, so the sibling real files were duplicate shadows whose presence broke test isolation (two `database_resolver`/event-bus copies); kept the thin facade + `auth/` + `data/`. (2) Repointed the `mfdb_admin` plugin at the relocated `mfdb.admin.*` package (manifest entrypoints + backend/GUI compat wrappers) and deleted the retired `sample_database` plugin. (3) Fixed a bad find-replace from the cutover that had rewritten five test modules' admin Python import paths `mfdb_admin → mfdb.admin`, producing the nonexistent hybrid `chisurf.plugins.core.mfdb.admin.*` and a hard `ModuleNotFoundError` (the "3 stale reds" flagged in the Merge-1 note, actually 5) — repointed to canonical `mfdb.admin.*`, unblocking 136 tests. Net effect: clean-HEAD `test/fio` went from **660 collection errors** (duplicate-module chaos) to a runnable suite; mfdb-core gate still 161 passed. **Newly exposed** by the now-running suite: ~163 `test/fio` failures across three root causes — global-state test-ordering leaks, `cs_settings`-not-flowing-into-standalone-`mfdb.config` decoupling (PRD-24, deferred), and a stale hardcoded `CREATE_TABLES_SQL` (`mfdb_operation` missing `protocol_id`/`protocol_version` vs the canonical `_TableDef`, Phase 3). Stabilization is the next step. Advances [INC-05](/specs/assessment.md#inc-05)/[PRD-24](/prds/prd-24.md).
* **MFDB simplification — Merge 2 in progress: god-class breakup begun.** Added identity-preserving `DictionaryDao.upsert` (INSERT…ON CONFLICT DO UPDATE — the safe write path the `add_*`/`save_*` family re-roots onto). Extracted pure JSON/time helpers into `mfdb/_sqlutil.py` (breaks the import cycle mixins would form). Splitting the 7,322-line `MFDatabase` god-class into per-concern mixins under `mfdb/queries/`: **ParameterMixin, StudyMixin, ProtocolMixin, LifecycleMixin, BranchMixin, UserDeviceMixin, ExperimentMixin** (7 concerns; public method surface unchanged — methods resolve via MRO, zero delegators); `repository.py` 7,322 → 6,389 lines. Shared pure helpers (`_row_to_dict`, `_exists` too) live in `_sqlutil`. Extracting LifecycleMixin exposed and fixed a latent bug in the `chisurf.core.mfdb` compatibility facade — it set `__path__` to the vendored package, so a submodule imported through the facade before `mfdb.X` loaded became a **duplicate module** (two event buses); a meta-path finder now aliases every `chisurf.core.mfdb.X` to the canonical `mfdb.X`, fulfilling the facade's "no duplicate class objects" contract. mfdb-core gate green throughout (now 161 passed, incl. test_lifecycle/test_events). Remaining: the tightly-coupled provenance **core** (probes/spectra/forster, samples, artifacts/operations/objects/edges, provenance-graph, setups/calibrations, entities, analysis-metadata) — more entangled, best extracted carefully — then Phase 3 (fully `.dic`-generated schema) and Merge 3 (import unification, ABC drop). Advances [INC-05](/specs/assessment.md#inc-05)/[PRD-26](/prds/prd-26.md).
* **MFDB simplification — Merge 1 (of 3) landed: one fewer access layer.** Started the MFDB de-monolith program (finishing [PRD-19](/prds/prd-19.md)/[PRD-26](/prds/prd-26.md)/[PRD-24](/prds/prd-24.md); target [specs/mfdb.md](/specs/mfdb.md); backlog [INC-05](/specs/assessment.md#inc-05)). (1) Tracked the previously-untracked `modules/mfdb/` tree (source + `.dic`/`spectra.db` data assets) in git so the package is versioned. (2) Added a public API for the private helpers callers reached into — `utc_now`, `json_loads`, `get_active_database`, `session_token_registry`, `hash_token` (private names kept as-is; purely additive). (3) **Deleted the SQLAlchemy `orm/` layer** (`orm/{base,models,sample_repository,sync}.py` + `test_orm.py`, ~2,700 lines): it duplicated the raw-SQL repository over the same tables and existed mainly to be kept in sync with it. Its one production consumer (`get_sample_full_description`'s ORM branch) already had a complete raw-SQL fallback, now the sole path; the three parallel access styles are now two. Remaining (Merge 2/3): standardise on the dictionary-driven `DictionaryDao` engine, add `DictionaryDao.upsert`, split the 7,322-line `MFDatabase` god-class into per-concern mixins, make the schema fully `.dic`-generated, unify the three import paths, drop the `base.py` ABC. mfdb-core test gate green throughout (103→118 passed). Pre-existing reds noted: 3 test files carry stale `chisurf.plugins.core.mfdb.admin.*` imports (→ fix in the import-unification merge).
* **PRDs fully migrated into OKF; `overhaul/` retired.** The `okf/prds/` group is now the **self-contained** home for every PRD: all 61 concepts were expanded from thin summaries to carry the full design text folded from their `overhaul/` sources (and companion docs — PRD-02 summary/completion/review, PRD-03/04 code reviews & punch-lists, PRD-10 bug note, PRD-31 pyqtgraph-migration), with curated frontmatter/status preserved (OKF wins on conflict), `# Source` sections dropped, `resource:` repointed at code, external products kept role-based, and no `overhaul/` links. Added two new PRDs from the retired working notes — [PRD-57 (ChiMOL command parity + renderer migration)](/prds/prd-57.md) and [PRD-58 (FRET FPS+OLGA superset)](/prds/prd-58.md) — plus the authoritative [PRD Implementation Order](/prds/master-order.md). Durable non-PRD `overhaul/` docs were rehomed as references: [MFDB architecture ideas](/references/mfdb-architecture-ideas.md), [MFDB LIMS diagnosis](/references/mfdb-lims-diagnosis.md), [node/workflow-toolkit lessons](/references/orange3-lessons.md), [modelling/ProteinMC roadmap](/references/modelling-roadmap.md); the MFDB dictionary programming rules folded into [architecture/mfdb.md](/architecture/mfdb.md), IDEA-01 (structure-aware sample definer) into [modelling](/plugins/modelling.md), and the background-estimation upgrade into [burst](/plugins/burst.md). Updated the prds/references/root indexes, `overview.md`, and `roadmap-prds.md` to drop `overhaul/` pointers, then deleted the `overhaul/` folder. Ephemeral `overhaul/` files (README/START_HERE/TODO/completion reports, superseded plans) were dropped as archive.
* **CLAUDE.md refresh + AGENTS.md folded in.** Added a "Working practices" policy — *mark work done in its tracker* (PRD `status:`/DoD, index glyph, assessment row) and *track every edit in OKF* (update the matching concept + append to this log). Folded the durable parts of the removed root `AGENTS.md` into CLAUDE.md (change policy, Qt/`--no-deps`/`python -m chisurf`/`PYTHONPATH` gotchas, docs build, testing & rendering conventions, imp-tricks split pointer, client-server plugin standard, object-store pointer to [architecture/mfdb.md](/architecture/mfdb.md)); stale bits were redirected to OKF rather than copied, and `overhaul/` references in CLAUDE.md were repointed at `okf/prds/`.
* **Notes integration**: Folded the loose top-level `notes/` working files into the bundle so their durable knowledge is curated rather than lost. New concepts: [ChiMOL viewer profile](/plugins/profiles/chimol.md) (PyMOL-style `cmd` surface + Qt-free renderer / ImGui-migration constraints, from the moview→chimol cmd-parity notes) and [Known issues & gotchas](/references/known-issues.md) (still-open functional bugs + recurring engineering pitfalls distilled from two working bug logs). Enriched existing concepts: [MFDB](/architecture/mfdb.md) gained an object-store & provenance-aware-reader section; [MFDB Admin](/plugins/profiles/mfdb-admin.md) gained the dictionary-driven `EntityDock` architecture; [Modelling](/plugins/modelling.md) gained durable FRET-engine facts (AV backends, `fps.json` keys, evaluators, OLGA reuse) + the superset roadmap pointer; [GUI & AutoForm](/subsystems/gui-autoform.md) gained the shared node-editor widget (versioned JSON graph schema + DAG); [Light Path Simulator](/plugins/profiles/lightpath-simulator.md) gained a preset-format / graph-normalization / macOS-combobox gotchas section. Verified against the tree first — the object store, mfdb-admin `EntityDock`, chimol `sele_parser`, and FRET evaluators have all landed; the node editor now lives at `chisurf/gui/widgets/node_editor`. Fixed-bug play-by-play and already-resolved TODOs were treated as archive and not copied.
* **MFDB Admin packaging**: Moved the MFDB Admin application into the MFDB package as `mfdb.admin` (backend, GUI, CLI, and JSON view specs). The ChiSurf `core/mfdb_admin` plugin now points its manifest and wrappers at `mfdb.admin`, and root package discovery no longer includes `modules/mfdb_admin/src` as a separate package source.
* **MFDB dictionary relocation**: Confirmed MFDB dictionary files (`*.dic`) live under `modules/mfdb/src/mfdb/data/`; the old `chisurf/core/mfdb/data` dictionary copy has been removed.
* **MFDB extraction**: Removed the ChiSurf `DataCurve` import from `mfdb.payload_models`; `GenericCurve` now serializes curve-like arrays and returns plain curve kwargs. Added boundary coverage so `payload_models`, `result_registry`, runtime config, and session/database resolution stay free of ChiSurf imports.
* **MFDB seed data**: Removed the ChiSurf fluorescence helper imports from `mfdb.seed_data`; seed Förster overlap/radius calculations are now local NumPy helpers with boundary coverage. Bundled the fluorophore reference `spectra.db` under `modules/mfdb/src/mfdb/data/` so seeded/reference imports no longer resolve from the ChiSurf `_dev` plugin path by default.
* **MFDB design issue**: Replaced `MFDatabase.add_probe_type()` `INSERT OR REPLACE` with a non-destructive upsert so reseeding/updating probe types preserves primary keys referenced by imported probes under foreign-key enforcement.
* **MFDB seed data API cleanup**: Updated curated seed-data entity creation to the current `MFDatabase.add_entity(entity_id, name, entity_type, details)` API instead of stale keyword arguments.
* **MFDB seed data API cleanup**: Updated curated seed-data sample-probe creation to use explicit `fluorophore_type`, `description`, and `poly_probe_position_id` keyword arguments instead of stale positional ordering.
* **MFDB repository API cleanup**: Extended `get_sample_probe_mappings()` to support all-mapping queries and `probe_id` filters, matching existing seed/admin/importer call shapes.
* **MFDB project archiver extraction**: Removed the ChiSurf experiment serialization import from `mfdb.project_archiver`; project curve arrays are encoded locally as dtype/shape/base64 dictionaries and covered by the MFDB import-boundary test.
* **MFDB sample manager design cleanup**: Updated tests and fallback behavior for the prerelease hard cutover: structured sample creation uses the SQLite graph adapter by default, SQLAlchemy remains optional, and raw-SQL full-description reads include a stable nullable `condition` object plus display-name-backed descriptions.
* **MFDB facade**: Aliased loaded `mfdb.*` submodules under `chisurf.core.mfdb.*` so transitional imports do not duplicate class objects. Added regression coverage for base, repository, payload codec, and payload models module identity.
* **MFDB Admin evidence**: Updated the current status to reflect green arm64 Qt verification: MFDB Admin plugin-local suite at 103 passed / 2 warnings and the adjacent MFDB Admin + Database Connector + Project Browser smoke at 117 passed / 2 warnings. GUI construction-only surfaces remain marked as not behavior-done in the coverage ledger.
* **MFDB Admin path contract**: Verified compile/import and focused tests with `PYTHONPATH="modules/mfdb/src:modules/chinet:modules/imp-tricks/src:."` and no `modules/mfdb_admin/src`. The integration/startup slice reported 60 passed, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 117 passed / 2 warnings.
* **MFDB Admin Measurements GUI**: Added sample-data-backed headless coverage for the Measurements aggregate panel. The test drives `MFDBWidget` in ChiSurf context, refreshes seeded raw/processing/processed rows, and verifies kind/search filtering. Fixed the client routing so generic processing and processed-product lists use `mfdb.processing.list` / `mfdb.processed_data.list`, added the `processed_data_id` alias to generic product rows, made operation listing status-aware, and removed stale undefined `_exc` logging paths from `mfdb.admin.gui.tool`. The plugin-local suite reported 105 passed / 2 warnings, the focused GUI/admin/database smoke reported 45 passed / 2 warnings, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 119 passed / 2 warnings.
* **MFDB Admin Provenance Graph GUI**: Added sample-data-backed headless coverage for loading a raw -> processing -> processed provenance graph through `MFDBWidget`. Fixed the shared test DB patch so split backend service modules (`measurement_services`, `ndxplorer_services`, `fluorophore_services`) resolve to the temporary MFDB, and canonicalized provenance graph seed node types so `processed_data` seeds do not create duplicate `processed_data:*` and `artifact:*` nodes. The plugin-local suite reported 107 passed / 2 warnings, the focused GUI/admin/database smoke reported 46 passed / 2 warnings, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 121 passed / 2 warnings.
* **MFDB Admin Import / Export GUI**: Added sample-data-backed headless coverage for the Import / Export panel: export validation, flrCIF preview, validation-warning confirmation, sample CIF export, sample-table CSV export, and minimal CIF file import through the GUI file field. Kept interaction-test widgets alive for the module to avoid Qt queued-callback crashes after C++ widget deletion. The plugin-local suite reported 108 passed / 2 warnings, the focused GUI/admin/database smoke reported 47 passed / 2 warnings, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 122 passed / 2 warnings.
* **Database Connector side effects**: Added temporary-file coverage for service-only `database_connector` backup, reset-from-source, minimal CIF import, and FLR CIF text/file export. The connector-local suite reported 4 passed; the focused GUI/admin/database smoke reported 48 passed / 2 warnings, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 123 passed / 2 warnings.
* **Project Browser deeper coverage**: Added sample-data-backed Project Browser tests for branch DAG roots/leaves, branch version counts, artifact browsing including project-contained fit results, fit-parameter listing, import collision rejection/remapping, delete manage-permission enforcement, and headless GUI delete/import actions. Fixed version graph root/leaf semantics for stored `newer -> parent` supersedes edges, made artifact browsing include `project_contains` artifacts, and repaired `.csp` import collision handling so object UUIDs are remapped through the deduplicating MFDB object store while preserving valid artifact storage vocabularies. The Project Browser plugin-local suite reported 10 passed, the focused GUI/admin/database smoke reported 54 passed / 2 warnings, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 129 passed / 2 warnings.
* **Project Browser restore GUI**: Added headless GUI coverage for restoring a selected MFDB project version into a patched ChiSurf context. The test uses temporary sample data, the real in-process Project Browser services, verifies `load_project_payload()` receives the restored `Project`, and checks active ChiSurf project metadata updates. The Project Browser plugin-local suite reported 11 passed / 1 warning, the focused GUI/admin/database smoke reported 55 passed / 2 warnings, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 130 passed / 2 warnings.
* **MFDB Admin Objects GUI**: Moved object-store Copy UUID, Reveal, and Delete object actions onto the visible Objects EntityDock and added sample-data-backed headless coverage for selecting the seeded object row, copying its UUID, revealing the object-store blob path, confirming deletion, and verifying MFDB object removal. The MFDB Admin plugin-local suite reported 109 passed / 2 warnings, the focused GUI/admin/database smoke reported 56 passed / 2 warnings, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 131 passed / 2 warnings.
* **MFDB Admin data-action GUI**: Moved raw-data, processed-product, and analysis read-only actions onto the visible EntityDock path and added sample-data-backed headless coverage for Copy ID, Reveal/Open where file-backed, and provenance-seed selection. The seeded raw and processed artifacts now use real temporary files so the open/reveal path is exercised against concrete local paths. The MFDB Admin plugin-local suite reported 110 passed / 2 warnings, the focused GUI/admin/database smoke reported 57 passed / 2 warnings, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 132 passed / 2 warnings.
* **MFDB Admin analysis drilldown GUI**: Added `mfdb.analysis.full` as a manifest-declared read-only RPC backed by `MFDatabase.get_analysis_run_full()`, added a visible Analysis Details action, and covered the non-modal parameter/output-product drilldown with seeded analysis data. Remaining analysis GUI gap is now non-empty input-product/grouped-fit drilldowns. The MFDB Admin plugin-local suite reported 110 passed / 2 warnings, the focused GUI/admin/database smoke reported 57 passed / 2 warnings, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 132 passed / 2 warnings.
* **MFDB Admin artifact validation/delete GUI**: Added repository-level artifact validation and soft-delete operations, exposed them as manifest-declared `mfdb.artifacts.validation.set` and `mfdb.artifacts.delete` RPCs, and moved validation/delete controls onto the visible Raw Data and Processed Products EntityDocks. Added sample-data-backed headless coverage for validation status changes, confirmed soft delete, table refresh, and operation-link soft-delete for seeded raw and processed artifacts. The MFDB Admin plugin-local suite reported 111 passed / 2 warnings, the focused GUI/admin/database smoke reported 58 passed / 2 warnings, and the broad MFDB Admin + Database Connector + Project Browser smoke reported 133 passed / 2 warnings.

## 2026-07-05
* **SV-03 fix**: Routed `chisurf.*` globals through `ChiSurfAPI` `SessionState` — API owns `self._state` aliasing the global lists, all API methods use `self._state.fits`/`self._state.datasets` instead of direct `cs.*` access. 84 tests pass. 12 total fixed.
* **DATA-03 fix**: Consolidated duplicated `mfdb_*` DDL into single-source `_TableDef`/`_Column` dataclasses — both permissive (migration) and CHECK-constrained (fresh-DB) DDL are generated from `_CANONICAL_TABLE_DEFS`. Removed hand-written `_CANONICAL_CHECK_SQL` and fragile string-matching `_CANONICAL_TABLE_MAP`. 2 guardrail tests. 11 total fixed.
* **DATA-02 fix**: Added ordered migration waterfall to MFDB schema versioning — `MIGRATIONS` `OrderedDict` (v1 fresh DB, v40 reconcile), `migrate_schema()` reads stored version and applies pending steps sequentially. 6 tests. 10 total fixed.
* **SV-04 fix**: Promoted service errors to the JSON-RPC `error` member — `ZmqServer._handle_one()` converts `ok: False` handler results to proper JSON-RPC error responses. `ChisurfClient.call()` now raises `RemoteError` for both application and transport errors. 14 tests updated. 9 total fixed.
* **S2 fixes**: Landed 3 S2 items — SV-02 (deleted dead DTO dataclasses), SV-05 (event topic contract in `server_methods.json` + guardrail), BUG-04 (`Model` ABCMeta enforcement). Updated assessment statuses.
* **S1 fixes**: Landed all 5 S1 items from the [assessment backlog](/specs/assessment.md) — BUG-01 (NCurve `elif` guard), BUG-02 (stale `fit_select` removed), BUG-03 (`component_type` NameError), SV-01 (Qt-free `chisurf/core/data_io/detector_setups.py`), DATA-01 (3 broken manifests + validation logging). Each committed separately with tests. Added guardrail tests: `test_no_duplicate_handler_names` (BUG-02), `test_no_gui_import_in_server` (SV-01), `test_builtin_manifests_all_valid` (DATA-01). Updated assessment statuses. Created `chisurf/core/data_io/` package.
* **Population**: Expanded OKF coverage from the code tree: added subsystem concepts for [fluorescence domain](/subsystems/fluorescence-domain.md), [project persistence](/subsystems/project-persistence.md), and [pipelines](/subsystems/pipeline.md); added plugin-group concepts for [TTTR tools](/plugins/tttr.md), [trajectory tools](/plugins/trajectory.md), and [sample/spectra/curation](/plugins/sample-spectra.md). Updated subsystem/plugin indexes and cross-links so existing populated pages are discoverable.
* **Integration**: Added the [prds](/prds/index.md) group — one OKF concept per PRD (persistent `PRD-NN` number as stable id, status + phase frontmatter) integrated from the `overhaul/` folder, which is kept intact as the authoritative full text (each concept's `resource` points back). References to external/third-party software are described by role, not named. Index grouped by implementation phase.
* **Migration**: Moved the former top-level `SPECS/` target specs into the OKF bundle as the [specs](/specs/index.md) group — `overview`, `core`, `rpc`, `mfdb`, `plugins`, the [assessment](/specs/assessment.md) backlog, and the [template](/specs/template.md). Converted each to an OKF concept (frontmatter + bundle-relative links) and cross-linked the target specs to their current-state concepts. Removed `SPECS/`.
* **Initialization**: Created the ChiSurf OKF bundle — root [index](/index.md), [overview](/overview.md), and the `architecture/`, `subsystems/`, `workflows/`, and `references/` groups.
